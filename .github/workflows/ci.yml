name: CI - Playwright Tests with Allure

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  test:
    name: Run Tests and Generate Allure Report
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run Playwright tests
        run: npm test
        continue-on-error: true

      - name: Generate Allure Report
        if: always()
        run: |
          npm run report:generate

      - name: Upload Allure Report as Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: allure-report/
          retention-days: 30

      - name: Generate Allure Report Screenshot
        if: always()
        run: |
          node scripts/screenshot-report.js

      - name: Upload Allure Report Screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report-screenshot
          path: allure-report-screenshot.png
          retention-days: 30

      - name: Add Screenshot to Job Summary
        if: always()
        uses: actions/github-script@v7
        env:
          REPOSITORY: ${{ github.repository }}
          RUN_ID: ${{ github.run_id }}
        with:
          script: |
            const fs = require('fs');

            const screenshotPath = 'allure-report-screenshot.png';
            if (fs.existsSync(screenshotPath)) {
              const repository = process.env.REPOSITORY;
              const runId = process.env.RUN_ID;
              
              const summary = '## 📊 Allure Test Report Screenshot\n\n' +
                '✅ **Screenshot generated successfully!**\n\n' +
                '📥 **Download the screenshot:** Check the **Artifacts** section on the right → Download `allure-report-screenshot`\n\n' +
                `📊 **View full report:** [Download Allure Report](https://github.com/${repository}/actions/runs/${runId})\n\n` +
                '🌐 **View online:** If GitHub Pages is enabled, report will be available at: `https://<username>.github.io/loginpage-allure/`\n\n' +
                '> 💡 **Tip:** The screenshot shows a visual summary of all test results, including pass/fail status and execution charts.';
              
              fs.appendFileSync(process.env.GITHUB_STEP_SUMMARY, summary);
            }

      - name: Comment PR with Screenshot
        if: always() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        env:
          REPOSITORY: ${{ github.repository }}
          RUN_ID: ${{ github.run_id }}
        with:
          script: |
            const fs = require('fs');

            const screenshotPath = 'allure-report-screenshot.png';
            if (fs.existsSync(screenshotPath)) {
              const imageBuffer = fs.readFileSync(screenshotPath);
              const imageBase64 = imageBuffer.toString('base64');
              const repository = process.env.REPOSITORY;
              const runId = process.env.RUN_ID;
              
              github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: `## 📊 Allure Test Report\n\n![Allure Report](data:image/png;base64,${imageBase64})\n\n[Download Full Report](https://github.com/${repository}/actions/runs/${runId})`
              });
            }

  deploy-report:
    name: Deploy Allure Report
    runs-on: ubuntu-latest
    needs: test
    if: always() && github.ref == 'refs/heads/main'
    permissions:
      contents: read
      pages: write
      id-token: write
    # Don't fail the workflow if this job fails
    continue-on-error: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download Allure Report Artifact
        uses: actions/download-artifact@v4
        with:
          name: allure-report
          path: allure-report/
        continue-on-error: true

      - name: Check if Pages is enabled
        id: check-pages
        uses: actions/github-script@v7
        continue-on-error: true
        with:
          script: |
            try {
              const response = await github.rest.repos.getPages({
                owner: context.repo.owner,
                repo: context.repo.repo,
              });
              core.setOutput('enabled', 'true');
              core.setOutput('url', response.data.html_url || '');
              console.log('✅ GitHub Pages is enabled');
            } catch (error) {
              core.setOutput('enabled', 'false');
              console.log('⚠️ GitHub Pages is not enabled');
              console.log('📝 Please enable it at: https://github.com/${{ github.repository }}/settings/pages');
            }

      - name: Setup Pages
        if: steps.check-pages.outputs.enabled == 'true'
        uses: actions/configure-pages@v4
        continue-on-error: true

      - name: Upload artifact
        if: steps.check-pages.outputs.enabled == 'true'
        uses: actions/upload-pages-artifact@v3
        with:
          path: allure-report/
        continue-on-error: true

      - name: Deploy to GitHub Pages
        if: steps.check-pages.outputs.enabled == 'true'
        id: deployment
        uses: actions/deploy-pages@v4
        continue-on-error: true

      - name: Deployment Status
        if: always()
        run: |
          if [ "${{ steps.check-pages.outputs.enabled }}" == "true" ] && [ "${{ steps.deployment.outcome }}" == "success" ]; then
            echo "✅ Report deployed successfully to GitHub Pages!"
            echo "🌐 View at: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"
          else
            echo "⚠️ GitHub Pages deployment skipped."
            echo ""
            echo "📝 To enable GitHub Pages:"
            echo "   1. Go to: https://github.com/${{ github.repository }}/settings/pages"
            echo "   2. Under 'Source', select 'GitHub Actions'"
            echo "   3. Save the settings"
            echo "   4. The report will deploy automatically on next push"
            echo ""
            echo "📥 You can still download the report from Artifacts section"
            echo "   Or use: npm run report:view (after downloading)"
          fi
